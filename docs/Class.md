``` mermaid
classDiagram
    class کاربر {
        +شناسه: عدد
        +نام: متن
        +ایمیل: متن
        +ورود()
        +خروج()
    }

    class مهمان {
        +جستجوی_هتل()
        +مشاهده_اتاق()
        +رزرو_اتاق()
        +پرداخت_هزینه()
        +ثبت_نظر()
    }

    class پذیرش {
        +بررسی_رزروها()
        +ثبت_ورود_مهمان()
        +ثبت_خروج_مهمان()
        +مدیریت_اتاق‌ها()
    }

    class مدیر_سیستم {
        +بررسی_گزارشات()
        +نظارت_بر_پرداخت‌ها()
        +مدیریت_کاربران()
    }

    class اتاق {
        +کد: عدد
        +نوع: متن
        +وضعیت: متن
        +قیمت: عدد
        +ویرایش_اتاق()
        +افزودن_اتاق()
        +حذف_اتاق()
    }

    class رزرو {
        +کد: عدد
        +تاریخ_ورود: تاریخ
        +تاریخ_خروج: تاریخ
        +وضعیت: متن
        +ثبت_رزرو()
        +لغو_رزرو()
    }

    class پرداخت {
        +کد: عدد
        +مبلغ: عدد
        +تاریخ: تاریخ
        +پرداخت_آنلاین()
    }

    class گزارش {
        +کد: عدد
        +نوع: متن
        +تاریخ: تاریخ
        +ایجاد_گزارش()
    }

    %% روابط
    کاربر <|-- مهمان
    کاربر <|-- پذیرش
    کاربر <|-- مدیر_سیستم

    پذیرش "1" o-- "*" رزرو : مدیریت رزروها
    مهمان "1" o-- "*" رزرو : ایجاد رزرو
    رزرو "1" o-- "1" اتاق : شامل اتاق
    رزرو "1" o-- "1" پرداخت : مرتبط با پرداخت
    مدیر_سیستم "1" o-- "*" گزارش : تولید گزارش
