``` mermaid
stateDiagram-v2
    [*] --> ثبت_درخواست_رزرو : شروع فرآیند توسط مهمان
    ثبت_درخواست_رزرو --> بررسی_موجودی_اتاق : رزرو ارسال شد
    بررسی_موجودی_اتاق --> تایید_رزرو : اتاق در دسترس است
    بررسی_موجودی_اتاق --> رد_رزرو : اتاق موجود نیست / اطلاعات ناقص
    تایید_رزرو --> پرداخت : منتظر پرداخت مهمان
    پرداخت --> تایید_نهایی_رزرو : پرداخت موفق
    پرداخت --> لغو_رزرو : عدم پرداخت یا لغو توسط مهمان
    تایید_نهایی_رزرو --> ورود_به_هتل : روز ورود مهمان
    ورود_به_هتل --> اقامت_فعال : مهمان در حال اقامت است
    اقامت_فعال --> خروج_از_هتل : پایان اقامت
    خروج_از_هتل --> ثبت_نظر : امکان ثبت نظر مهمان
    ثبت_نظر --> پایان : رزرو تکمیل شد
    رد_رزرو --> پایان : فرآیند تمام شده (رزرو مردود)
    لغو_رزرو --> پایان : فرآیند لغو رزرو

    %% توضیحات (Notes)
    note right of ثبت_درخواست_رزرو
      مهمان درخواست رزرو را برای تاریخ مشخص ثبت می‌کند
    end note
    note right of بررسی_موجودی_اتاق
      سیستم یا پذیرش موجودی اتاق‌ها را بررسی می‌کند
    end note
    note right of تایید_رزرو
      در صورت موجود بودن اتاق، رزرو اولیه تأیید می‌شود
    end note
    note right of پرداخت
      پرداخت هزینه رزرو به‌صورت آنلاین انجام می‌گیرد
    end note
    note right of تایید_نهایی_رزرو
      پس از پرداخت، اتاق نهایی و آماده‌ی ورود مهمان می‌شود
    end note
    note right of ورود_به_هتل
      پذیرش ورود مهمان را در سیستم ثبت می‌کند
    end note
    note right of اقامت_فعال
      وضعیت رزرو در زمان اقامت مهمان فعال است
    end note
    note right of خروج_از_هتل
      پذیرش خروج مهمان را ثبت می‌کند
    end note
