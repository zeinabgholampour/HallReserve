
🎭 سناریوی رزرو سالن

بازیگران (Actors)

• کاربر (User): کسی که قصد رزرو سالن دارد.

• سیستم رزرو سالن (System): نرم‌افزاری که فرایند ثبت‌نام، ورود و رزرو را مدیریت می‌کند.


---

جریان اصلی (Main Flow)

1. شروع ثبت‌نام
   
 • کاربر وارد صفحه ثبت‌نام می‌شود.
 
• اطلاعات شخصی (نام، ایمیل، رمز عبور) را وارد می‌کند. 

• سیستم حساب کاربری ایجاد کرده و پیام موفقیت نمایش می‌دهد.

2. ورود کاربر

• کاربر با ایمیل و رمز عبور وارد سیستم می‌شود.

• سیستم اعتبارسنجی اولیه انجام می‌دهد.

3. تأیید هویت
   
• سیستم کد تأیید به ایمیل یا شماره موبایل کاربر ارسال می‌کند.

• کاربر کد را وارد کرده و هویت او تأیید می‌شود.


4. مشاهده سالن‌های قابل رزرو

• سیستم لیست سالن‌ها را با جزئیات (ظرفیت، امکانات، وضعیت رزرو) نمایش می‌دهد.

• کاربر سالن مورد نظر را انتخاب می‌کند.

5. انتخاب ساعت و تاریخ

• سیستم جدول زمان‌بندی سالن را نشان می‌دهد.

• کاربر ساعت و تاریخ آزاد را انتخاب می‌کند.

6. تأیید رزرو

• سیستم خلاصه رزرو (سالن، تاریخ، ساعت، هزینه) را نمایش می‌دهد.

• کاربر رزرو را تأیید کرده و در صورت نیاز پرداخت آنلاین انجام می‌دهد.

7. صدور رسید و اعلان

• سیستم رسید رزرو را صادر کرده و به ایمیل/پیامک کاربر ارسال می‌کند.

• رزرو در پروفایل کاربر ذخیره می‌شود.



---

جریان‌های جایگزین (Alternative Flows)

• اگر کاربر رمز عبور را فراموش کند → سیستم امکان بازیابی رمز عبور فراهم می‌کند.

• اگر سالن در زمان انتخابی پر باشد → سیستم پیام خطا داده و گزینه‌های جایگزین پیشنهاد می‌دهد.

• اگر پرداخت ناموفق باشد → سیستم رزرو را نهایی نمی‌کند و کاربر باید دوباره تلاش کند.


---

نتیجه (Outcome)

کاربر با موفقیت سالن مورد نظر را در تاریخ و ساعت دلخواه رزرو کرده و رسید دریافت می کند.
