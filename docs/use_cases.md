🎯 سناریوی رزرو سالن کنفرانس

1. نقش‌ها

• کاربر عادی (Employee): کسی که می‌خواد سالن رو رزرو کنه.
• مدیر سالن (Admin): مسئول تأیید یا رد رزروها.
• سیستم: مدیریت زمان‌بندی و جلوگیری از تداخل رزروها.


2. جریان سناریو

1. ورود کاربر به سیستم:
کاربر با نام کاربری و رمز عبور وارد می‌شود.
2. انتخاب تاریخ و ساعت:
کاربر تاریخ مورد نظر (مثلاً ۲۰ دی ۱۴۰۴) و بازه زمانی (۱۰ صبح تا ۱۲ ظهر) را انتخاب می‌کند.
3. بررسی دسترس‌پذیری سالن:
سیستم بررسی می‌کند که آیا سالن در آن بازه زمانی آزاد است یا خیر.• اگر آزاد باشد → مرحله بعد
• اگر رزرو شده باشد → پیام خطا نمایش داده می‌شود و کاربر باید زمان دیگری انتخاب کند.

4. ثبت درخواست رزرو:
کاربر اطلاعات جلسه (عنوان، تعداد شرکت‌کنندگان، تجهیزات مورد نیاز مثل پروژکتور یا وایت‌برد) را وارد می‌کند.
5. تأیید مدیر سالن:
مدیر سالن درخواست را مشاهده کرده و می‌تواند آن را تأیید یا رد کند.• در صورت تأیید → رزرو نهایی می‌شود و نوتیفیکیشن برای کاربر ارسال می‌گردد.
• در صورت رد → دلیل رد شدن (مثلاً تعمیرات سالن) به کاربر اطلاع داده می‌شود.

6. مدیریت تغییرات:
کاربر می‌تواند رزرو خود را لغو یا ویرایش کند (مثلاً تغییر ساعت یا تجهیزات).


3. سناریوی نمونه

• کاربر “علی” وارد سیستم می‌شود.
• تاریخ ۲۰ دی ۱۴۰۴، ساعت ۱۰ تا ۱۲ را انتخاب می‌کند.
• سیستم بررسی می‌کند و سالن آزاد است.
• علی درخواست رزرو برای جلسه “معرفی محصول جدید” با ۱۵ نفر و نیاز به پروژکتور ثبت می‌کند.
• مدیر سالن درخواست را تأیید می‌کند.
• رزرو در تقویم سیستم ثبت می‌شود و علی نوتیفیکیشن دریافت می‌کند.

می‌خوای برات یک فلوچارت ساده هم طراحی کنم تا جریان رزرو رو تصویری نشون بده؟
